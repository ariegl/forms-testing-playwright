# Instrucciones de Salida para CLI
- Siempre que te pida código (JSX, PowerShell, CSS), devuelve ÚNICAMENTE el código puro.
- PROHIBIDO incluir explicaciones, introducciones o comentarios fuera del código.
- PROHIBIDO usar bloques de código de Markdown (evita los triple backticks ```).